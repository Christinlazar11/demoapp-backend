export enum SubmissionStatus {
    Pending = 'pending',
    Approved = 'approved',
    Rejected = 'rejected',
  }

export enum EmailTemplateSubject{
    submission =`Learner's Application`,
    updation = `Learner's Application Update`
}